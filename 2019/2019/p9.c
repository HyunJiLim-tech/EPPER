#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int m[30000];
int m_max[30000];

void find_max(int num) {
	
}

int main() {
	int n;

	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		scanf("%d", &m[i]);
}